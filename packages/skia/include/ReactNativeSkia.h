#pragma once

// Umbrella header for the SwiftPM target's publicHeadersPath. Nothing outside
// this package includes Skia or RNSkia headers — React Native resolves the
// Fabric component and TurboModule by class name — so the public surface is
// deliberately empty.
