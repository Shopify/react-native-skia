import type { BlurMaskFilterProps } from "../dom/types";

import type { DrawingContext } from "./Context";

export const renderBlurMaskFilter = (
  _ctx: DrawingContext,
  _props: BlurMaskFilterProps
) => {
  "worklet";
  throw new Error("Not implemented");
};
