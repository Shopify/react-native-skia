// We need to remove the native version of NativeSetup - to avoid typescript errors.
// The file should not do anything, so to avoid typescript errors we export a dummy variable
export const dummy = 0;
