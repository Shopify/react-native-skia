import React from "react";

import type { SkiaProps } from "../../processors/Animations/Animations";

export const LumaColorFilter = (props: SkiaProps<void>) => {
  return <skLumaColorFilter {...props} />;
};
