// The documentation is served from https://shopify.github.io/react-native-skia,
// so every route lives under this prefix (in development too, to match
// production). Next's <Link> applies it automatically; raw URLs such as
// <img src> or fetch() calls must prefix it themselves.
export const basePath = "/react-native-skia";
